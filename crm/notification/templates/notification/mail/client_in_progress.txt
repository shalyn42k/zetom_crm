{% comment %}Автописьмо клиенту: документ перешёл в статус "in_progress".
   Используется триггером Б из crm/notification/signals.py для всех трёх
   типов документов — тип передаётся переменной document_kind, чтобы
   подставить корректное слово.
   Контекст рендера:
       document       — Oferta / Zlecenie / Wniosek
       parent         — document.from_main (RequestMain)
       document_kind  — "оферта" | "заказ" | "заявление"  (русский)
       document_code  — короткий код, например "OFR-2026-0042"
   Первая строка — тема. Шаблон-черновик.{% endcomment %}
{{ document_code }}: ваш {{ document_kind }} принят в работу

Здравствуйте{% if parent.first_name %}, {{ parent.first_name }}{% endif %}!

Сообщаем, что ваш {{ document_kind }} {{ document_code }} принят в работу.
По заявке {{ parent.company_name|default:"" }} ({{ parent.created_at|date:"d.m.Y" }}).

Мы свяжемся с вами при изменении статуса либо при необходимости уточнить детали.
Если у вас появятся вопросы — просто ответьте на это письмо.

С уважением,
команда Zetom
