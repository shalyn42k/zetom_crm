{% comment %}Письмо стаффу: пользователь нажал "Mail" по существующему Wniosek.
   Контекст рендера:
       document  — Wniosek
       parent    — document.from_main (RequestMain)
       sender    — request.user
   Первая строка — тема. Шаблон-черновик.{% endcomment %}
Поступило заявление в работу: WNI-{{ document.created_at|date:"Y" }}-{{ document.pk|stringformat:"04d" }} ({{ parent.company_name|default:"—" }})

Здравствуйте.

По заявке REQ-{{ parent.created_at|date:"Y" }}-{{ parent.pk|stringformat:"04d" }} от {{ parent.company_name|default:"клиента" }} оформлено заявление (Wniosek).

Реквизиты заявления:
  Внутренний номер: WNI-{{ document.created_at|date:"Y" }}-{{ document.pk|stringformat:"04d" }}
{% if document.application_number %}  Номер заявления у клиента: {{ document.application_number }}{% endif %}
  Контактное лицо: {{ document.full_name|default:parent.full_name }}
  Телефон: {{ document.phone|default:parent.phone }}
  Email клиента: {{ document.email|default:parent.email }}

Заметки:
{{ document.notes|default:"—" }}

После отправки этого письма статус заявления в CRM меняется на "Ожидание" (waiting).

Инициатор отправки: {% if sender %}{{ sender.get_full_name|default:sender.username }}{% else %}система{% endif %}
