# Шаблон документации модуля: [Название модуля]
Этот шаблон стоит создать в начале того как вы создали ветку, и обновлять по мере работы над этой веткой.

1.**Статус:** [В разработке / На проверке / Слито]

2.**Автор:** [Имя]

3.**Ветка:** `feature/[название]`

---

## 1. Функциональное назначение

Краткое описание задачи модуля.

*Пример: Модуль предотвращает создание повторных заявок на основе хеширования ключевых данных (Email, Телефон, Текст).*

## 2. Алгоритм работы (Логика)
Пошаговое описание процессов:
1. [Шаг 1, например: Получение данных из POST-запроса].
2. [Шаг 2, например: Генерация хеша по 3 полям].
3. [Шаг 3, например: Поиск совпадений в БД среди активных заявок].
4. [Результат: Ошибка 409 при дублировании или создание новой записи].

## 3. Схемы и диаграммы
Упоминанание того нужна ли диаграма, если да то куда вы ее скинули и когда чтобы можно было к ней вернуться.
Желательно кидать диаграму в папку **`zetom_crm/DOCS/DIAGRAMS/`**.

## 4. Изменения в базе данных
Список затронутых таблиц и новых полей:
* `Table: requests` -> Поле `request_hash` (тип: string, unique).
* `Table: requests` -> Поле `last_modified_by` (ForeignKey на User).

## 5. Информационная безопасность (ISO 27001)
* **Логирование:** Описание того, какие действия записываются в лог (UserID, IP, Тип события, Timestamp).
* **Целостность:** Как модуль защищает данные от некорректного изменения.

## 6. Матрица прав доступа
Права ролей в рамках данного модуля:

| Роль | Права доступа |
| :--- | :--- |
| **Глава Отдела** | Полный доступ (CRUD), игнорирование блокировок. |
| **Старший подопечный** | Создание, редактирование, просмотр. |
| **Бомж** | Только создание заявок. |
| **Всезнайка (душнила)** | Только просмотр логов и истории изменений (Read-only). |

## 7. Инструкция для мануального тестирования
Сценарий для проверки «человечком под тесты»:
1. [Действие 1].
2. [Действие 2].
3. **Ожидаемый результат:** [Описание того, что должно произойти].

---

### Чек-лист перед созданием Pull Request(обязательно чтобы понимать че вы делали):
- [ ] Выполнен `git pull origin main`.
- [ ] Выполнен `git pull origin test`.
- [ ] Конфликты разрешены локально.
- [ ] Создан/обновлен MD-файл документации в папке `DOCS/`.
- [ ] Использован ИИ(если да то пишите где-то сверху где)