# New Bugs

> Последнее обновление: 2026-06-24

Статусы: ⚠️ ЧАСТИЧНО · ❌ НЕ ИСПРАВЛЕН

---

### ⚠️ БАГ-1 — Нет кнопки редактирования клиента прямо из заявки

| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` → блок Linked Clients |
| **Проблема** | Рядом с привязанным клиентом есть только крестик (отвязать) и ссылка-текст на имени клиента (открывает в новой вкладке). Отдельной выделенной edit-кнопки с иконкой карандаша нет |
| **Ожидается** | Кнопка-иконка (карандаш/edit) рядом с каждым клиентом, открывающая форму клиента без выхода из заявки |

---

### ⚠️ БАГ-2 — Неполная история заявки (только смены статуса)

| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` → блок истории |
| **Проблема** | `StatusHistory` фиксирует только смены статуса. Не фиксируются: кто изменил поля, кто добавил/убрал пользователя, кто привязал клиента, кто отправил письмо |
| **Частично** | `django-simple-history==3.11.0` установлен (`requirements.txt`) и настроен в middleware, но `HistoricalRecord` ни к одной модели не подключён |
| **Что нужно** | Добавить `history = HistoricalRecord()` в модель `RequestMain` или писать кастомные записи при каждом действии |

---

### ⚠️ БАГ-3 — Английские слова в польском интерфейсе

**Переведено:** `Notifications→Powiadomienia`, `Inbox→Skrzynka`, `Requests→Zlecenia`, `Klienci`, `Phone→Telefon`, `Created at→Utworzono`, `First name→Imię`, `Submit your request→Wyślij swoje zgłoszenie`, `Filter→Filtr`.

**НЕ переведено** (~40 строк в `locale/pl/LC_MESSAGES/django.po`, `msgstr ""`):

| Элемент | Английский текст | Должно быть (PL) | Где находится |
|---|---|---|---|
| Поиск в сайдбаре | `Search apps and model...` | `Szukaj aplikacji i modeli...` | Левый сайдбар |
| Кнопки темы | `Light` / `Dark` | `Jasny` / `Ciemny` | Попап пользователя |
| Плейсхолдер поиска | `Type to search` | `Wpisz, aby wyszukać` | Поле поиска в списке клиентов |
| Заголовок колонки | `Kind` | `Rodzaj` | Таблица уведомлений |
| Заголовок колонки | `Recipient` | `Odbiorca` | Таблица уведомлений |
| Заголовок колонки | `Title` | `Tytuł` | Таблица уведомлений |
| Заголовок колонки | `Is read` | `Przeczytano` | Таблица уведомлений |
| Поле | `Actor` | `Wykonawca` | Форма детали уведомления |
| Поле | `Template name` | `Nazwa szablonu` | Форма детали уведомления |
| Поле | `Rendered` | `Wyrenderowano` | Форма детали уведомления |
| Поле | `Payload` | `Dane` | Форма детали уведомления |
| Поле | `Target content type` | `Typ obiektu docelowego` | Форма детали уведомления |
| Поле | `Target object id` | `ID obiektu docelowego` | Форма детали уведомления |
| Поле | `Read at` | `Przeczytano o` | Форма детали уведомления |
| Заголовок колонки | `Subject` | `Temat` | Таблица email-уведомлений |
| Заголовок колонки | `Recipient email` | `E-mail odbiorcy` | Таблица email-уведомлений |
| Заголовок колонки | `Sent at` | `Wysłano` | Таблица email-уведомлений |
| Бейдж роли | `AUDITOR` | `AUDYTOR` | Карточка пользователя |
| Название пермишена | `View users` | `Przeglądaj użytkowników` | Panel uprawnień |
| Название пермишена | `Edit users` | `Edytuj użytkowników` | Panel uprawnień |
| Название пермишена | `View roles` | `Przeglądaj role` | Panel uprawnień |
| Название пермишена | `Edit roles` | `Edytuj role` | Panel uprawnień |
| Название пермишена | `View requests` | `Przeglądaj zgłoszenia` | Panel uprawnień |
| Название пермишена | `Edit requests` | `Edytuj zgłoszenia` | Panel uprawnień |
| Название пермишена | `Delete requests` | `Usuń zgłoszenia` | Panel uprawnień |
| Вкладка | `Users` | `Użytkownicy` | Страница ролей |
| Вкладка | `Roles` | `Role` | Страница ролей |
| Заголовок колонки | `Code` | `Kod` | Таблица ролей |
| Заголовок колонки | `Name` | `Nazwa` | Таблица ролей |

**Отдельная проблема — роли без `gettext_lazy`:**

| Элемент | Английский текст | Должно быть (PL) | Где находится |
|---|---|---|---|
| Метки ролей у пользователей | `specialist` | `specjalista` | Список сотрудников в валидации |
| Метка роли | `all_seeing` | `widzący wszystko` | Список сотрудников в валидации |
| Иерархия ролей | `admin › dep_head › owner › specialist` | `admin › kierownik działu › właściciel › specjalista` | Правая панель в валидации |
| Роль в дропдауне | `Auditor` | `Audytor` | Поле "Rola" |
| Название роли (данные) | `custom role` | `rola niestandardowa` | Таблица ролей |
| Название роли (данные) | `Specialist` | `Specjalista` | Таблица ролей |
| Название роли (данные) | `Department Head` | `Kierownik działu` | Таблица ролей |

Имена ролей в `users/signals.py` — обычные строки без `gettext_lazy`. Перевод невозможен без рефакторинга: добавить `gettext_lazy` в определения и добавить `msgid` в `.po`.

---

### ❌ БАГ-4 — Не фиксируется кто создал заявку

| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` |
| **Проблема** | В модели `RequestMain` нет поля `created_by` / `author`. Нигде не отображается кто и когда создал заявку |
| **Что нужно** | 1. Добавить `created_by = ForeignKey(User, null=True, on_delete=SET_NULL)` в `RequestMain` |
| | 2. В `save_model` при создании записывать `request.user` |
| | 3. Отображать в шаблоне карточки заявки |

---

### ❌ БАГ-5 — Заявка без отдела не назначается главному администратору

| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` |
| **Проверено** | REQ-2026-0042 создана без отдела → "Przypisani użytkownicy 0", "Działy 0" |
| **Проблема** | Через Validation Window поле `departments` обязательное (`required=True`). Но при прямом создании `RequestMain` через admin — заявка зависает без ответственного, никто не получает уведомление |
| **Что нужно** | В `save_model` или через сигнал: если `departments` пустой → добавить в `assigned_to` пользователя с ролью `admin` |

---

### ❌ БАГ-6 — Нет возможности прикрепить файл к заявке

| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` и публичная форма `/zetom/email/` |
| **Проблема** | В модели `RequestMain` нет поля для файловых вложений. Нельзя прикрепить документ, скан, договор |
| **Что нужно** | Новая модель `RequestAttachment(request, file, uploaded_by, uploaded_at)` + виджет в форме |

---

### ❌ БАГ-7 — Нет сканирования документов

| | |
|---|---|
| **Где** | Публичная форма `/zetom/email/` и карточка заявки |
| **Проблема** | Нет возможности отсканировать и прикрепить физический документ |
| **Зависит от** | БАГ-6 — сначала нужна загрузка файлов |

---

### ❌ БАГ-8 — Email-форма создаёт заявку без файловых вложений

| | |
|---|---|
| **Где** | `/zetom/email/` → создаёт `RequestNull` |
| **Проблема** | `views.py` обрабатывает только `request.POST`, `request.FILES` не используется. Модель `RequestNull` не содержит `FileField`. Если заявка пришла с документом — файл нигде не сохраняется |
| **Зависит от** | БАГ-6 |

---

### ❌ БАГ-9 — Нет истории контактов с клиентом

| | |
|---|---|
| **Где** | Карточка заявки и карточка клиента |
| **Проблема** | Нет модели для фиксации контактов с клиентом. Нельзя записать: позвонили, написали письмо, была встреча |
| **Что нужно** | Новая модель `ClientInteraction(request, client, channel, summary, contacted_by, contacted_at)` |

---

### ❌ БАГ-10 — Нет фиксации канала, сути и участников контакта

| | |
|---|---|
| **Связан с** | БАГ-9 |
| **Проблема** | Нет структуры для: как связались (канал), о чём говорили (суть), с кем конкретно (сотрудник + контактное лицо клиента) |
| **Что нужно** | Каждая запись контакта: `channel` (звонок/письмо/встреча/чат), `summary`, `employee`, `contact_person` |

---

### ❌ БАГ-11 — Нет автоматических напоминаний каждые 48 часов

| | |
|---|---|
| **Где** | Фоновые задачи |
| **Проблема** | В проекте нет Celery, нет cron-задач, нет periodic tasks. Клиент не получает напоминание если заявка не завершена |
| **Ожидается** | Каждые 48 часов по незакрытым заявкам автоматически уходит email клиенту |
| **Что нужно** | Подключить Celery + Celery Beat, написать задачу `remind_unfinished_requests()` которая раз в сутки проверяет заявки и отправляет напоминание если прошло 48ч без изменений |

---

### БАГ-7 — SMTP: ошибка отправки email на публичной форме

| | |
|---|---|
| **Где** | `/zetom/email/` — публичная форма создания заявки |
| **Ошибка** | `Notification failed: [Errno -3] Temporary failure in name resolution` |
| **Проблема** | Сервер не может подключиться к SMTP — DNS не резолвит хост почтового сервера |
| **Ожидается** | Заявка создаётся и email-уведомление уходит успешно |
| **Вероятная причина** | Неверный `EMAIL_HOST` в `.env` на сервере, или у сервера нет доступа к внешней сети / DNS |
| **Что проверить** | `EMAIL_HOST`, `EMAIL_PORT`, `EMAIL_HOST_USER` в `.env` на сервере |

---

### БАГ-6 — Нет кнопки редактирования клиента прямо из заявки  
## Уже сделано Никитой 
| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` → блок Linked Clients |
| **Проблема** | Рядом с привязанным клиентом есть только крестик (отвязать). Кнопки редактирования (карандаш/edit) нет |
| **Ожидается** | Рядом с каждым клиентом — кнопка редактировать, открывающая форму/popup с данными клиента без выхода из заявки |
| **Сейчас** | Нужно идти в `/admin/clients/client/<id>/` отдельно, редактировать там, возвращаться |

---

### БАГ-5 — Не фиксируется кто создал заявку

| | |
|---|---|
| **Где** | `/admin/zetom/requestmain/<id>/change/` |
| **Проблема** | В заявке нигде не отображается кто её создал. Поля `created_by` / `author` нет ни в модели, ни в форме, ни в шаблоне |
| **Ожидается** | В карточке заявки видно: кто создал, когда создал |
| **Что нужно** | 1. Добавить `created_by = ForeignKey(User)` в модель `RequestMain` |
| | 2. В `save_model` при создании записывать `request.user` |
| | 3. Отображать в шаблоне карточки заявки |

---

### БАГ-2 — Кнопки статусов отображаются у Specialist

| | |
|---|---|
| **Кто** | Alan (Specialist) |
| **Где** | `/admin/zetom/requestmain/<id>/change/` → блок Status |
| **Проблема** | Кнопки New / In Progress / Waiting / Done / Apply видны, но при нажатии ничего не происходит |
| **Ожидается** | Кнопки скрыты или задизейблены без `change_request_status` |

---

### БАГ-3 — Freeform mail отправляется в обход прав и видимости ⚠️

| | |
|---|---|
| **Кто** | Любой staff-пользователь (проверено: Alan — Specialist) |
| **Где** | POST `/admin/zetom/requestmain/<id>/mail/freeform/` |
| **Проблема** | Эндпоинт не проверяет пермишен `send_documents` и не применяет фильтр видимости |
| **Ожидается** | 403 если нет `send_documents` или заявка не видна пользователю |
| **Риск** | Любой staff может отправить письмо от имени компании любому клиенту, зная только ID заявки |

**Как воспроизвести** — под Alan в DevTools Console (F12):
```javascript
fetch('/admin/zetom/requestmain/39/mail/freeform/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/x-www-form-urlencoded',
    'X-CSRFToken': document.cookie.match(/csrftoken=([^;]+)/)[1]
  },
  body: 'subject=test&body=test+message'
}).then(r => console.log('Status:', r.status, r.url))
```

---

### БАГ-4 — Request Review отправляется в обход прав и видимости ⚠️

| | |
|---|---|
| **Кто** | Любой staff-пользователь (проверено: Alan — Specialist) |
| **Где** | POST `/admin/zetom/requestmain/<id>/request-review/` |
| **Проблема** | Эндпоинт не проверяет пермишен `request_review` и не применяет фильтр видимости |
| **Ожидается** | 403 если нет `request_review` или заявка не видна пользователю |
| **Риск** | Любой staff может отправить review-уведомление любому пользователю на любую заявку, зная только ID |

**Как воспроизвести** — под Alan в DevTools Console:
```javascript
fetch('/admin/zetom/requestmain/39/request-review/', {method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','X-CSRFToken':document.cookie.match(/csrftoken=([^;]+)/)[1]},body:'note=test&recipient_ids=19'}).then(r=>console.log('Status:',r.status,r.url))
```

---
| **Проблема** | В проекте нет Celery, нет cron-задач. Клиент не получает напоминание если заявка не завершена |
| **Что нужно** | Celery + Celery Beat, задача `remind_unfinished_requests()` — раз в сутки проверяет незакрытые заявки и отправляет напоминание если прошло 48ч без изменений |
